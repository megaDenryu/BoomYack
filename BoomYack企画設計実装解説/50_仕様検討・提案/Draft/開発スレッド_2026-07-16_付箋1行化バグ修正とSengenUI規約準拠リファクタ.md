# 開発スレッド_2026-07-16: 付箋1行化バグ修正とSengenUI規約準拠リファクタ

対象リポジトリ: `submodules/Fudaba`(前半)、`submodules/BoomYack`(後半、本編)

## 経緯サマリ(前半: Jimbo/Fudaba関連、本編とは別件)

会話の前半は本ドキュメントの主題(BoomYack)とは別件。要点のみ記録:

1. Jimbo MCPが「Property keys should match pattern」400エラーで接続失敗 → 原因は `submodules/Fudaba/packages/mcp/src/Fudabaツールを登録する.ts` のMCPツール入力スキーマのプロパティキーが日本語(`種別`/`タイトル`等)だったため。Anthropic API側はinput_schemaのキーにASCIIしか許容しない。キーをASCII化(`kind`/`title`等)し、Fudaba REST APIとの実際の通信では日本語フィールド名へ内部マッピングする形で解決(REST契約自体は変更なし)。
2. ついでに`配置物/付箋`配下の旧世代デッドコード(付箋VM・旧自動リサイズ付箋View・スクロール可能モード一式)を削除し、生きていた`自動リサイズテキストエリア.ts`/`style.css.ts`を`付箋2/`へ移動。
3. Jimbo.exeの起動診断で1時間ハマった件: 原因はClaude Code自身がElectronベースであるため`ELECTRON_RUN_AS_NODE=1`を環境に強制セットしており、それを引き継いだままパッケージ済みexeを起動するとNode CLIモードになりウィンドウが出ない、というもの。これは**Jimbo自身のCLAUDE.mdに既に文書化されていた既知の罠**(`run-electron.bat`経由で回避)であり、事前に確認していれば数秒で済んだ話。教訓: 「exeを動かす」系タスクの前にCLAUDE.mdの当該節を先に確認すること。

## 本編: 付箋1行化バグとSengenUI規約準拠リファクタ

### ユーザー: 付箋の最小高さ調整依頼(発端)
> ここの最小高さが80になってましたが、１行の時は16+20+16で52なのでこれより小さかったからだめだったのかも？52にしてみた。

`自動リサイズテキストエリア.ts`の`テキストエリアサイズパラメータ`コンストラクタのデフォルト値(80→52)をユーザーが手動編集。ビルド・起動を依頼された。

### AI: 調査 → デフォルト値は死んでいた
コード追跡の結果、`自由テキストコンテンツView.ts`が`new テキストエリアサイズパラメータ().setMinHeight(依存関係.最小高さ)`という形で**必ず**上書きしており、コンストラクタのデフォルト値は実質どこにも効いていないことが判明。実際に効いている値は`CanvasItemFactory.ts`の`create付箋`にハードコードされた`new Px長さ(50)`だった。

### ユーザー: 「じゃあまずは見た目を変えますか」
**判断**: `CanvasItemFactory.ts`側の値を名前付き定数化して52へ変更する方向で合意。ビルド・起動して確認。

### ユーザー: 「変わらないね」→「バックスペースを1回しかおしてないわけないでしょ」
1行にならない問題が継続。AIは「末尾の改行文字が残っているのでは」と誤った仮説を提示し、ユーザーから強く否定された(長押しで全消去済み)。

### AI自問: 推測をやめてログで実測する
**問い**: 「これ以上推測で当てずっぽうに直すと、前回セッションの二の舞になるのでは？」
**結論**: `console.log`を`adjustHeight()`・キー入力ハンドラ・枠のonHeightChangeに仕込んで実測する。
**理由**: ユーザーから「ログを仕込んでください」と明示依頼もあった。

ログの結果、**空文字(`""`)のときだけ`scrollHeight`が2行分(72px)を返す**ことが判明。改行を含む非空文字列では常に正しい値が返っていた。原因はplaceholder(`"付箋の内容を入力..."`)の描画がscrollHeightに混入するChromiumの既知の挙動。

### 発見: SengenUIに既存の対処メソッドがあった
`TextAreaC.autoFitToContent()`というメソッドが既に存在し、ドキュメントコメントに全く同じバグへの言及があった(空文字時はinline heightを外してCSS min-heightへ委ねる、という対処)。これを採用して自前のscrollHeightロジックを削除。

**しかしこれだけでは不十分だった**(後述、2回の回帰の原因)。

### 品質ループ的な流れ: SengenUI規約準拠リファクタへ拡大
ユーザーから「BoomYack全体がSengenUI最新規約(1ファイル100行・配線パターン等)に準拠していない、この機会にリファクタしてほしい」との依頼。Explore agentで棚卸しした結果:
- 134ファイル中30+が100行超
- `点ハンドルViewBase`の継承ツリーがSealed原則(継承禁止)違反
- `自動リサイズテキストエリア.ts`が生DOM `addEventListener`を使用
- 「配線オブジェクト+`配線する()`」パターンがBoomYack全体で0件(旧来のoption内コールバック方式のまま)

実施した項目(コミット済み):
1. Sealed違反解消: `点ハンドルViewBase`+3継承クラス(円/点/終点矢印ハンドルView)を`Iハンドル形状`インターフェース注入のコンポジションへ書き換え。副産物として`中点ハンドルView.ts`の不安全キャスト(`as unknown as`)と、実体化されない死んだ形状クラスも解消。
2. 生DOM排除: `自動リサイズテキストエリア.ts`/`テキストフォーマッタサービス.ts`を`TextAreaC`経由に書き換え。SengenUI側に`高さPxを取得する()`/`値と選択範囲を書き換えて通知する()`を新設。
3. 型定義集約ファイルの分割: `データクラス.ts`(718行)・`I配置物.ts`(281行)・`グラフVM標準.ts`(306行)を概念ごとに単一責務ファイルへ分割。既存の`折れ線矢印View.ts`と同じ「barrel後方互換」パターンを踏襲し、元ファイルはre-exportのみのbarrelとして残すことで、既存の29+ファイルのimportパスを一切変更せずに済ませた。

### ユーザー: 「ちょっと今壊れてるのでストップです」
ここまでの変更をコミットした直後にストップがかかった。

### AI: 原因切り分け(git checkout比較)
`git switch --detach`で今日の変更前のコミット(`52d2cb2`)へ一時的に戻してビルド・起動し、同じ症状が出るか確認する方式で切り分け。

### ユーザー: 具体的な症状報告
> BoomYackアプリを起動したときに一番最初に自動配置される付箋の描画が崩れています。(中略)何も入力されてないときは、黄色の着色領域は本来の1行付箋のサイズと全く同じだが、devtoolで見ると実際にはTextAreaは2行になっている。

**判明した仕組み**: `付箋グラフボード.ts`に「付箋ディスペンサー」機構があり、ボード起動時に左上へ自動で1枚生成され、それをドラッグすると同じ位置に新しい付箋が即座に生成される(`付箋をめくる動作`)。この最初の付箋は`StickyGraphBoard`のコンストラクタ内で**documentへの接続前に同期的に**生成される。

**回帰の原因**: `高さPxを取得する()`(`offsetHeight`の実測)は、要素がdocumentへ未接続の段階では信頼できない値を返す。加えて`TextAreaC.autoFitToContent()`の空文字時対処(inline heightを外してCSS min-heightへ委ねるだけ)は、placeholderの自然な折り返し高さ(2行分)がmin-heightを上回ると効かない(min-heightは「下限」でしかない)。

**修正**: 空文字時は`autoFitToContent()`を使わず、`setStyleCSS`で高さを明示的に`minHeight`へ固定し、`onHeightChange`へ渡す値も実測に頼らず既知の`minHeight`をそのまま使う。

### ユーザー: 「治りました」→ さらに深掘りへ
一旦直ったことを確認後、ユーザーから重要な指摘:
> もともと付箋の黄色の領域は変わった作り方になっていたはず。それはドラッグするための兼ね合いでgradiationみたいなので疑似的に黄色に見せたいところを無理やり黄色に見せてるみたいな感じだったと思う。(中略)このアプリの設計上の最大の問題は演出のためにdom構造がゆがめられてるところではある。

`付箋ホバー領域`(外枠)が`background: linear-gradient(色,色) content-box`という手法で、paddingを含む領域全体は透明のまま、content-box(=中身の実サイズ)だけに色を塗る「疑似着色」をしていたことが判明。**「見えている黄色い付箋」と「実際に色が適用されている領域」が別々の計算経路(CSS content-box算出 vs JSのサイズ計算)を持つ構造**であり、サイズ変更のたびに両者が食い違うリスクを常に抱えていた(今回の回帰の遠因)。

### AI: 演出をやめて実要素に置き換える提案 → 実施
**判断**: グラデーション芸をやめ、外枠のpadding分だけ内側にぴったり収まる**実サイズ・実背景色を持つ「付箋本体」div**を新設する設計に変更。

### ユーザー: 「なんで既存ファイルをいじってるの？新しいファイルで作れ」
> ぶっちゃけリファクタよりも０から作りなおす方がいい気もします。(中略)デザインとユーザー目線のインターフェースはそのままに、コードを置き換える感じです。

最初、既存の`自動リサイズ付箋View.ts`を直接編集して対応していたが、ユーザーから強く却下された:
> 新しいクラスとして作りなおして、`new 自動リサイズ付箋View`してるところを変えればいいやろって意味に決まってるやろ。

**判断**: 既存ファイルへの変更を`git checkout --`で完全に破棄し、新規ファイル`付箋枠.ts`(+`付箋枠オプション.ts`/`付箋枠コンテキストメニュー.ts`/`付箋ジオメトリ.ts`/`付箋枠style.css.ts`)として作り直す方式に転換。旧`自動リサイズ付箋View.ts`は無改変のまま参照実装・切り戻し先として残す。呼び出し元(`付箋集約.ts`はじめ計6ファイル)の`new 自動リサイズ付箋View`/型参照だけを新クラスへ差し替えた。

また選択時の緑/赤の枠線について:
> 緑の枠矩形は黄色の周りにするのではなく、ハンドルを包むようにしてください。もともとはそうなってました。

最初の書き直しで誤って外枠(ハンドル込み領域)から付箋本体(黄色領域のみ)へ移してしまい、ユーザーの指摘で外枠に戻した。**選択枠線は「ハンドルを含む操作可能領域全体」を示す意図であり、見えている色領域と一致させるべきではない**、という設計意図がここで明確になった。

### ユーザー: 「なんで0ベースで作ってるのに100行以上あるの」
`付箋枠.ts`が514行のままだったことへの指摘。SengenUI規約(1ファイル100行目安)を「新規ファイルだから」と免除されるわけではない、という当然の指摘。

**対応**: `付箋枠.ts`を4ファイルへ再分割:
- `付箋枠オプション.ts`(38行): コンストラクタオプション・コンテキストメニュー依存関係の型定義
- `付箋枠コンテキストメニュー.ts`(87行): 右クリックメニューの組み立て・マイク状態連動を関数として抽出
- `付箋ジオメトリ.ts`(69行): 位置・サイズから重心・矢印接続ポイントを導出する純粋計算クラス(使い捨てで毎回`new`する設計)
- `付箋枠.ts`(338行): DOMツリー構築・ドラッグ/リサイズ処理・Transform確定ロジック本体

主クラスはまだ100行を超えているが、右クリックメニューと座標計算を追い出した分小さくなった。**残タスクとして記録**: `付箋枠.ts`本体のさらなる分割(配線パターンの導入を含む)は未着手。

## 未解決の問題(次のタスク)

### 1. 矢印接続点(青丸ハンドル)の位置ズレ — 最優先・未修正
付箋を右クリックで選択・ホバーした際に表示される4方向の矢印接続点(接続点.ts)が、付箋の実際の位置と全く違う場所に散らばって表示される。

**有力な仮説(未検証)**: `接続点.ts`は`setViewportPositionByTransform()`で`position:fixed`にしているが、これはCSSの仕様上、**祖先要素に`transform`が設定されていると、"fixed"なのに実際にはビューポートではなくその祖先要素基準になる**という罠がある。付箋自体(`付箋ホバー領域`)が`transform: translate(...)`で位置指定されているため、接続点の"fixed"配置がその付箋を基準に化けている可能性が高い。

ただし`相対pos`の補正計算(`calculate矢印接続ポイント相対Transform`)がこの罠を意図的に打ち消す設計になっている可能性もあり(初期配置はviewport基準、以後の更新は`setTranslate`で相対基準に切り替わる二段構え)、静的解析だけでは正確な原因・修正方法を確信できていない。**devtoolsでの実測(該当要素のcomputed style・実際の座標)が必要**。

この問題は「ハンドルの作り方」全般についてユーザーが「CSS演出でDOM構造が歪められている最大の問題箇所」と指摘した象徴的な箇所でもあり、次の設計検討でも中心的な論点になる見込み。

### 2. `付箋枠.ts`本体(338行)のさらなる分割・配線パターン導入
今回は「DOM歪みの解消」を最優先にスコープを絞り、配線パターン(`配線オブジェクト`+`配線する()`)の導入は意図的に見送った。理由: 2つの変更を同時に行うと、今回のような回帰が起きたときにどちらが原因か切り分けられなくなるため。今回のDOM構造修正が安定してから着手する。

### 3. BoomYack SengenUI規約準拠リファクタの残タスク(元の作業リストより)
500行級の他のLV2クラス(`CanvasView.ts`537行・`セーブパネル.ts`575行・矢印系477+405行等)への同種のリファクタ横展開は未着手。`付箋枠.ts`シリーズで確立したパターン(部品集約への分割・configビルダー関数の抽出・純粋計算クラスの分離)を横展開する想定。

## 現在のファイル構成(付箋の「枠」関連、2026-07-16時点)

生きているファイル:
- `付箋2/付箋枠.ts` — 枠本体(旧`自動リサイズ付箋View`の置き換え)
- `付箋2/付箋枠オプション.ts` — オプション型
- `付箋2/付箋枠コンテキストメニュー.ts` — 右クリックメニュー組み立て
- `付箋2/付箋ジオメトリ.ts` — 座標計算
- `付箋2/付箋枠style.css.ts` — 付箋本体のスタイル(新設)
- `付箋2/自動リサイズ付箋style.css.ts` — 付箋ホバー領域等の既存スタイル(変更なし)

無改変で残置(参照実装・切り戻し先):
- `付箋2/自動リサイズ付箋View.ts` — 旧実装。`付箋選択状態` enumの定義元として型だけ引き続き参照されている(クラス本体はどこからも`new`されていない)

## 検証状況
各ステップごとにビルド(`npm run build:electron`)→ `run-electron.bat`起動を実施し、コンパイルエラーが無いことは確認済み。ただし**実機での矢印接続点の見た目確認は次のタスクとして持ち越し**。

## 追記: 仕様復元の基準コミット訂正

### ユーザー: CSSに埋もれた動作仕様を先に確立したい
> cssにコードからはddd的に読み取れない動作仕様が埋もれており、それのせいでコードを修正するとそこが壊れるという構造です。なのでリファクタするためには動作仕様をまず確立する必要があると考えています。

### AI: 既存実装から仕様を復元する方針を提示
CSS、DOM階層、座標計算、ブラウザのレイアウト処理に分散した前提を、不変条件として回収してからリファクタする方針を提示した。

### ユーザー: CSSに埋もれた仕様の復元を依頼
> あなたならcssに埋もれた仕様も復元できると思っています。なのでやってみてください。

### ユーザー: 正常動作の基準コミットを指定
> 正しい正解はgit上では、00fe0b0bda1c55c5827d5fdad44361d0fadd46cbの「fix: SengenUI規約準拠リファクタで発生した付箋高さの回帰を修正」までです。そのあとの１コミットは前のAIの修正が入っていて壊れてるので正しくないです。

**判断**: BoomYackコミット`00fe0b0bda1c55c5827d5fdad44361d0fadd46cb`を正常動作の正本とする。後続コミット`8113de5`の`付箋枠`実装は仕様の根拠に使わない。作業ツリーを切り替えず、`git show`で正本と同時点のSengenUIコミット`500442957ab870b3f200f97aa1d97c7645a0723d`を調査した。

### AI: 暗黙仕様と偶然の成立条件を分離
調査により、黄色面が`content-box`グラデーションで作られていることに加え、接続点の初期座標がtextareaの`setTimeout(0)`による高さ通知を契機に絶対座標から付箋ローカル座標へ上書きされ、結果的に正常化する時間依存を特定した。

復元結果は`設計2026-07-16_付箋の表示・操作仕様復元.md`へ記録する。正常コミットに存在する挙動を一律に仕様扱いせず、確定仕様、復元仕様、要確認・不具合候補へ分類する。

### ユーザー: 見えている結果はすべて意図された仕様
> ちなみにたまたまそう見えるものは何もないですよ？全部そう見えてほしいからそう書いてます。しかしもっと良いcssの書き方があるかもしれないというだけです。

**判断**: AIの「たまたま成立している見た目」という捉え方を訂正する。正常コミットで利用者から見える外観・操作結果はすべて意図された保存対象である。置き換えてよいのはcontent-boxグラデーション、透明padding、包含ブロック、非同期通知等の実現機構だけであり、変更前後の見た目と操作感は一致させる。静的解析で結果を一意に決められない箇所は、不具合扱いせず正常コミットを実測して結果を確定する。

コミット履歴も確認し、外枠を明示高さからCSSのauto高さへ変えた`11720b7`がユーザー実機確認で「全然ダメ」と判定され、`fcd3fbe`で差し戻されていたことを確認した。コード上で寸法が一致して見える方式でも、透明余白・選択枠・ハンドル・背景・初期描画を含む全体挙動は同等にならない前例として仕様書へ記録した。

### ユーザー: DOM構造と付箋召喚仕様が問題の上流にある
> 私の認識としては、dom構造に問題があるから、異常なcss技法や、実行順序を伴っているだとおもっています。それから、付箋召喚方法は仕様に問題があったりするから異常なことになってると思っています。

**判断**: 外観仕様、実現機構、製品操作仕様を分離する。現在の付箋の外観は保存対象だが、付箋ディスペンサー等の召喚方法は維持を前提にしない再設計対象とする。「生成UIを実付箋そのもので表現する」ことが、document接続前のtextarea・接続点・高さ計測を必要にし、DOM構造、補償CSS、実行順序依存を生む上流原因になっている。

第一候補として、召喚UIを「まだ配置物ではない生成コマンド」として実付箋集約から分離し、クリックまたはドラッグ開始時に通常の生成経路で初めて付箋を作る設計を仕様書へ追加した。具体的な召喚操作は実装前にユーザーと決定する。

### ユーザー: 実装開始を承認
> ではやりましょう

**判断**: 左上の付箋置き場の見た目とドラッグ体験を維持しつつ、召喚UIを実付箋から分離する。先に仕様文書をコミットし、壊れた後続コミット`8113de5`は`git revert`で履歴上打ち消した。

### AI: 付箋召喚UIを非配置物として実装
`付箋召喚UI.ts`とstyleを新設した。DOMは「透明な操作領域→実背景を持つ付箋本体→下書きtextarea」とし、旧content-boxグラデーションを使わず同じ見た目を再現した。召喚UIはモデル、保存、接続点、選択、衝突判定へ参加しない。

ドラッグ開始時に初めて`CanvasView.自由テキスト付箋を生成する()`から実付箋を作り、キャンバスメニューの自由テキスト生成も同じメソッドへ合流させた。下書き文字列は実付箋へ渡し、召喚UIは空状態へ戻る。旧`StickyGraphBoard._一番上の付箋`と`付箋をめくる動作`を削除した。

### AI: HTML既定のrows=2を実測で特定
召喚テスト中、空付箋は52pxだが1行の非空文字を持つ実付箋が72pxになることを確認した。computed styleとDOM値を測定すると、textareaの`rows`未指定によりHTML既定値2が使われ、`height:auto`時の`scrollHeight`が2行分になっていた。

`自動リサイズテキストエリア`と召喚UIのtextareaへ`rows: 1`を明示した。再ビルド後、空文字・1行下書き・生成後の1行実付箋がすべて200×52pxになることを確認した。

### 検証結果
- `npm run build:electron`: 成功
- 起動直後: 召喚UIのtextarea 1個、実付箋0個
- 召喚UI: 操作領域260×112、実背景本体200×52
- ドラッグ後: 召喚UIは元位置に残り、下書きを持つ実付箋が1枚生成
- 青い4接続点: 本体各辺から15px外側
- Undo後: 実付箋だけが消え、召喚UIは残る

### AI: 初期寸法を生成経路から分離
`CanvasItemFactory`に散在していた自由テキスト、タイトル付き、札参照の初期サイズと最小高さを
`付箋初期寸法.ts`へ集約した。新規生成と保存データ再読込が同じ定義を参照するようにし、再読込時
だけ自由テキスト付箋の最小高さが50pxになる差異を解消した。

### ユーザー: 枠線の責務と黄色部分をドラッグ不可にした理由を説明

> 付箋召喚UIをホバーしても緑枠が出ないのでどこをつかめバイいのか分からないです。緑枠と赤枠は
> 「ここはドラッグ可能な範囲だよ」というのをユーザーに教えるための責務があります。一方で、
> 付箋の黄色の部分は意図的にドラッグできないようにしてます。

> ダブルクリックしないと文字入力できないのでそこがめんどいなと思ったからです。しかしこれが、
> 付箋のdom構造とcssのゆがみを発生させてしまいました。ドラッグ可能な板が黄色の付箋の下にいれば
> いいやと思ったのですが、htmlでは上下関係にはイベントの面で意味があり、いろいろな矛盾に
> ぶち当たりました。

**判断**: 黄色い本体を直接編集可能・ドラッグ不可にする製品仕様は維持する。問題は製品仕様ではなく、
ドラッグ板を本体の背面へ重ね、padding、content-box背景、z-index、イベント伝搬停止で穴を作っていた
実現方法にある。緑・赤の枠線もドラッグ操作領域の表示契約として明文化する。

### AI: 操作領域を上下左右4本の明示的ハンドルへ変更

`付箋ドラッグ操作領域`を新設し、付箋座標シェルの中で黄色い本体と兄弟にした。操作領域は上下左右
30pxの4本のハンドルからなり、中央にはドラッグ用DOMが存在しない。4本のイベントは共通親へ
バブルさせるため、`PointerWife`は一つのまま維持した。

実付箋からcontent-boxグラデーションとroot paddingを削除し、黄色い背景を実寸の`付箋本体`へ設定した。
召喚UIも同じ操作領域を使用する。30pxと緑・赤・薄緑の枠線定義は`付箋操作仕様.ts`へ集約した。

実行時調査で、外周から黄色部分へ移動した後も選択管理の緑状態が残ることを発見した。操作領域の
`mouseleave`時に、その付箋自身が現在のホバー対象である場合だけ解除するよう修正した。また未選択時の
inline `outline: none`を外し、現在位置を示す緑枠はCSS`:hover`、赤・薄緑は選択状態が所有するようにした。

### 検証結果（ドラッグ操作領域分離）

- `npm run build:electron`: 成功
- 召喚UI外周: 緑破線、黄色部分: 枠なし
- 召喚後の実付箋: 赤破線
- 実付箋外周: 緑破線、黄色部分: 枠なし
- 黄色部分からのドラッグ操作: 位置不変、textareaへフォーカス
- 外周から100pxドラッグ: 付箋が100px移動
- 寸法: 操作領域260×112、本体200×52

### ユーザー: 緑枠の仕様責務を再訂正

> 付箋本体をホバーしてるときも緑の枠は出てたと思います。
> 接続点も緑枠の内側にありますが、ここにホバーしてるときも当然緑枠は出ます。
> 要は緑枠の内側にいるときは緑枠は出るということです。

**訂正**: 緑枠を外周ハンドルのヒットテスト表示と解釈したのは誤り。緑枠は現在ホバー対象の付箋全体と、
外周にドラッグ可能領域があることを示す。黄色本体、接続点、外周のどこにポインタがあっても表示する。
ただし、実際のドラッグ開始は上下左右の操作ハンドルに限定し、黄色本体の直接編集性は維持する。

`mouseleave`での解除とCSS`:hover`依存を削除し、付箋全体の`mouseover`を選択管理へ通知する形に戻した。
選択管理は同じ付箋への重複通知を無視する。これにより接続点などの子要素間を移動しても、解除と再設定が連続せず、緑枠の高速点滅が起きない。

### AI: 最新SengenUI規約に合わせた召喚UIの分割

`SengenUIガイド.md`と`SengenUI頻出パターン集.md`を再確認し、付箋召喚UIの上向きイベントをコンストラクタから外した。
`付箋召喚UI`は`LV2部品集約Base`とし、入力欄は単一要素のLV1拡張、ドラッグ操作領域は配線可能なLV2素部品とした。
三者のファイル行数はそれぞれ83行、48行、59行で、1ファイル100行以内を満たす。

### ユーザー: ホバー枠修正後の動作を確認し、継続を依頼

> 続けてください。動作はいい感じでした。

**判断**: 緑枠の表示範囲、黄色面の直接編集、外周だけをドラッグ開始領域とする現在の挙動を、
以後の分割作業で保存する基準とする。付箋機能を一度に全面改変せず、付箋2ディレクトリを
最初の完了単位として100行制限へ移行する。

### AI: 付箋の表示、寸法、部品構築、集約操作を分離

`自動リサイズ付箋View`から、座標と寸法を管理するレイアウト、DOM部品の構築、接続点計算、
選択状態、コンテキストメニュー、集約操作を分離した。View本体は公開契約の委譲に限定した。
コンテンツ初期化中に高さ通知が同期発火しても未構築部品を参照しないよう、接続点更新は
部品構築完了まで実行しない。

`自動リサイズテキストエリア`は`TextAreaC`のLV1拡張と配線ポートへ変更した。空文字時は
既知の最小高さを直接設定・通知し、非空時だけ内容に合わせて計測する。これにより基準コミット
`00fe0b0`で直した、未接続DOMの実測値とplaceholder自然高が混ざる問題を再導入しない。

テキストのインデント処理と括弧編集、タイトル付き表示、札参照表示も責務ごとに分離した。
札参照カードの高さ取得は生DOMの`scrollHeight`参照を廃止し、SengenUIが公開する
`getBoundingClientRect()`を利用する。

### 現在の検証結果と残作業

- 付箋2ディレクトリ内のTypeScriptファイル: すべて100行以下
- `タイトル付きコンテンツView.ts`: 100行
- `札参照コンテンツView.ts`: 69行
- `自動リサイズ付箋View.ts`: 58行
- `npm run build:electron`: 成功
- BoomYack全体で100行を超えるコードファイル: 42個

付箋2の行数制限は達成したが、最新版SengenUI規約への完全準拠は未完了。
`自動リサイズ付箋View`とコンテンツViewには、上方向イベントをオプションで受ける旧契約と、
LV2部品を含むのに`LV2部品集約Base`を使っていない箇所が残る。次は動作確認済みの挙動を
維持しながら、部品DTO、サービスDTO、必須配線ポートへ移行する。

### AI: 付箋Viewを最新版SengenUIの部品集約契約へ移行

`自動リサイズ付箋View`を`LV2部品集約Base<付箋View部品, 付箋Viewサービス>`へ変更した。
コンストラクタは、レイアウト状態、未配線の部品DTO、配線サービス、ルート構築の4段階を踏む。
ルート構築では、ドラッグ操作領域、コンテンツ、左右リサイズハンドルをDOMへ配置する式の中で
`配線する()`。旧`付箋View内部`は、部品の生成・配線・DOM配置・メニュー生成を一関数に
混在させていたため削除した。

上方向イベントは`I自動リサイズ付箋View配線`へ全件を必須列挙した。選択とホバーの登録用
メソッド、およびリサイズハンドルの`PointerWife`公開を廃止し、配線ポートへ統一した。
付箋集約は全イベントを必ず配線し、キャンバス側で処理が定義されていない任意イベントだけを
集約境界で明示的に無視する。

自由テキスト、タイトル付き、札参照の各コンテンツViewも、コンストラクタでは初期データだけを
受け、文字変更、高さ変更、編集確定、フォーカスを`I付箋コンテンツView配線`で通知する。
各コンテンツが使う自動リサイズ入力欄はLV1拡張なので、これらはLV2素部品として分類する。

### 検証結果（SengenUI配線契約への移行）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- 付箋2ディレクトリ内のTypeScriptファイル: 引き続きすべて100行以下
- `自動リサイズ付箋View.ts`: 96行
- `付箋Viewサービス.ts`: 63行
- `タイトル付きコンテンツView.ts`: 100行

BoomYack全体の100行制限とSengenUI完全準拠は引き続き未完了。次の対象は、100行超過が最大の
描画キャンバス、セーブパネル、矢印集約群から、依存関係と利用頻度を見て選定する。

### ユーザー: 作業後のElectron再起動と札場確認を依頼

> 作業が終わったらJimboの開発サーバーのエレクトロンは再起動するようにしてほしい。
> あと、Jimboの札場も確認してほしい。未着手札を何個か作った。
> PuppetGIFAnimatorのやつは別リポジトリだから無視していいよ。

**判断**: 今後の作業区切りでは、ビルド・差分検査・コミット後に`npm run dev:electron`の
Electron開発プロセスを再起動する。札場はAgentRoom内蔵API
`GET http://localhost:7100/api/fudaba/items`から読み取り、PuppetGIFAnimator関連は対象外とする。

確認したJimboの未着手札は、#55 右サイドバー仕様再検討、#54 Fudaba入力UI刷新、
#53 滑らか矢印のホバー／右クリック、#7 BoomYack規約リファクタ残。現在の依頼と同じ#7を
継続し、同じ矢印領域を触る#53を合わせて実施する。#54と#55は独立した大規模設計単位のため
今回の差分へ混ぜない。

### AI: 滑らか矢印の操作領域とホバー責務を直線矢印へ合わせる

原因は、直線矢印が見た目4pxとは別に20pxの線分ハンドルを持つ一方、滑らか矢印は見た目3pxの
SVGストローク自体だけをポインタ判定に使っていたこと。また滑らか矢印Viewの`hover()`と
`unhover()`は空実装で、ホバー色と中間点ハンドル表示を更新していなかった。

滑らか矢印を、3pxの表示パスと透明な20pxの操作パスへ分離した。操作パスがクリック、
ホバー、右クリックを受け、表示パスは通常、未選択ホバー、選択中、選択中ホバーの4状態を
直線矢印と同じ配色で描く。既存の右クリックによる曲線中央への中間点生成は維持し、
中間点が存在する場合は選択中またはホバー中だけ表示する。

`なめらか曲線矢印View`は`LV2部品集約Base`と必須配線ポートへ移行した。集約本体から、
衝突矩形・曲線中点・接続点再選択、永続化、中間点管理を分離した。始点・終点ハンドルの
重複していた接続・ドラッグ・接触判定は`曲線端点ハンドルBase`へ統合した。

### 検証結果（滑らか矢印）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- 滑らか曲線矢印ディレクトリ: 全TypeScriptファイル100行以下
- 集約: 212行から72行
- 始点／終点ハンドル: 124／120行から各15行
- BoomYack全体の100行超過コードファイル: 42個から38個

ローカルブラウザ自動操作に必要な実行機能はこのセッションへ公開されていないため、実動確認は
Electron再起動後にユーザー環境で行う。確認点は、曲線の上下約10pxでホバー色が変わること、
同じ範囲の右クリックで中間点が生成されること、中間点がホバー／選択中だけ表示されること。

### ユーザー: #53完了後も他タスクを継続

> 完了にしときました。じゃあ他タスクもやってほしいです

札#7の100行制限とSengenUI規約準拠を継続し、直前に比較対象として読んだ折れ線矢印の
View層を次の単位に選んだ。折れ線矢印本体はクリックとホバーの登録メソッドを公開しており、
構築後に任意の順序でイベントを足せる旧契約だった。点ハンドルは注入されたSVG形状を
暗黙フィールドとして扱い、線分ハンドルはCSSの5状態をView内の分岐で逐次付け外ししていた。

折れ線矢印本体を`LV2部品集約Base`へ移し、始点、終点、中点リスト、線分リストを部品DTOへ
明記した。選択とホバーは`I折れ線矢印View配線`の必須イベントとし、集約で一度だけ配線する。
点ハンドルも形状と内側コンテナを部品DTOにまとめ、使用されていなかった回転角度フィールドを
削除した。線分ハンドルのCSS状態判定は`線分ハンドル表示状態`へ分け、ドラッグ中、選択中、
ホバー中の三値から常に一つのクラスを決定する。色、判定幅、イベントの伝播順は変更しない。

### 検証結果（折れ線矢印View層）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- `線分ハンドルView.ts`: 128行から96行
- `点ハンドルView.ts`: 101行から69行
- `折れ線矢印View本体.ts`: 125行から98行
- `線分ハンドル表示状態.ts`: 25行
- BoomYack全体の100行超過コードファイル: 38個から35個

点・線分ハンドルが受け取る旧コマンド配列は残っている。ここは始点、終点、中点のラッパーと
矢印集約本体へ配線変更が及ぶため、次の独立した移行単位とする。

### ユーザー: #7のリファクタ継続

> 続けてください

前節で残したハンドルの旧コマンド配列を次の単位とした。旧
`Iハンドル操作実行時コマンド`は4メソッドがすべて任意で、Viewが配列をコンストラクタで
受け取って任意呼び出ししていた。実際の配列は全箇所で要素が一つだけであり、複数コマンドを
合成する仕様は存在しなかった。また、同じViewを使う滑らか矢印にもこの契約が波及していた。

`IハンドルView配線`を新設し、ドラッグ開始、ドラッグ中、ドラッグ終了、右クリックを必須化した。
点・線分ハンドルViewは`I配線可能`と`配線ポート`を使い、コンストラクタでは見た目だけを作る。
始点、終点、中点の各ラッパーは配線を内部の点ハンドルへ転送する。折れ線矢印の各集約と、
滑らか矢印の共通端点・中間点は、View生成後に一度だけ4イベントを配線する。端点右クリックの
ように処理を持たない経路も、未定義プロパティにはせず集約境界で明示した。

### 検証結果（ハンドル必須配線）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- `Iハンドル操作実行時コマンド`の参照: 0件
- 対象Viewと滑らか矢印ハンドル関連: すべて100行以下
- BoomYack全体の100行超過コードファイル: 35個のまま

上位の矢印集約が公開する`onハンドルドラッグ開始`と`onハンドルドラッグ終了`はまだ任意代入式で、
CanvasItemFactoryが生成後に設定している。View内部の配線とは別の集約間通知なので、次の独立した
設計単位で生成時注入または専用ポートへの変更を検討する。

### ユーザー: 滑らか矢印の中間点を折れ線矢印と同じく複数化

> 続けてください。あと、滑らか矢印が１つしか中間点打てないですこれはおかしい。
> 直線矢印と違うところです。合わせて。

原因はViewだけでなく、中間点管理、集約、曲線制御点計算がすべて単一の`handle | null`を前提に
していたこと。さらに、滑らか矢印の中間点は保存データへ含まれず、保存・再読込やUndo/Redoで
形状を維持できない状態だった。

中間点を順序付きリストへ変更した。曲線上の右クリック位置をSVGのローカル座標へ変換し、現在の
各ベジェ区間を32分割で評価して最も近い位置と区間を求める。その区間位置へ新しい中間点を挿入する
ため、既存点の前後関係を保ったまま何個でも追加できる。各ハンドルの右クリックは、そのハンドル
自身を管理リストとDOMから削除する。

曲線仕様は次で固定する。

- 中間点0個: 従来の始点・終点から自動計算するS字曲線
- 中間点1個: 従来の「中間点を厳密に通る」3次ベジェと同じ形状
- 中間点2個以上: 始点・全中間点・終点を順番に通る連続3次ベジェ

1個時は従来のベジェを中間点位置で2区間へ数学的に分割し、見た目を変えずに2個目の挿入先を
前後で判別できるようにした。2個以上は隣接点から接線を計算する区分3次ベジェとし、すべての
ハンドル位置を曲線が通る。終点矢印の角度は最終区間の接線から計算する。

`なめらか曲線矢印データ`へ`middlePoints`を追加し、VM、ファクトリ、シリアライズ、状態復元へ
通した。旧JSONではこの項目が存在しないため、読込時は空配列として扱う。中間点は選択中または
ホバー中のどちらか一方を満たす間は表示し続け、新しく追加・復元した点にも同じ表示条件を適用する。

### 検証結果（滑らか矢印の複数中間点）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- 滑らか曲線矢印ディレクトリ: 全TypeScriptファイル100行以下
- `なめらか曲線矢印データ.ts`: 98行
- BoomYack全体の100行超過コードファイル: 35個のまま

Node単体での曲線検証はSengenUI読込時にブラウザDOMを要求するため実行できなかった。最終確認点は、
同じ滑らか矢印へ3個以上を順番に追加できること、各点を個別にドラッグ・削除できること、保存後の
再読込で個数と順序が残ること。

### ユーザー: #7のリファクタを継続

> では続けてください

前節まで残っていた矢印集約の`onハンドルドラッグ開始`と`onハンドルドラッグ終了`を対象にした。
この2プロパティは任意で、CanvasItemFactoryが集約生成後に代入していた。未代入でもハンドル操作が
無反応のまま進むほか、折れ線と滑らか矢印で開始データ保持、終了データ比較、コマンド生成が重複
していた。

`I矢印集約配線`を追加し、開始・終了通知を必須化した。`I始終点矢印集約`は`I配線可能`を継承し、
各具象集約は`配線ポート`を一つ持つ。端点、中点、線分ハンドルは任意プロパティを呼ばず、集約の
必須通知メソッドを呼ぶ。未配線状態でユーザー操作が届けば配線ポートが明示的に失敗させる。

共通の`矢印ドラッグ履歴を作る`を追加した。開始時にシリアライズし、終了時に差分がある場合だけ
コマンドを積む責務を、矢印データ型に依存しない21行の部品へ集約した。`矢印Factory`は折れ線・
滑らか矢印の生成、データからのVM復元、履歴配線を担当する。CanvasItemFactoryはこの専用Factoryへ
委譲し、矢印集約を直接生成する場所を一箇所へ限定した。

### 検証結果（矢印集約の必須配線）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- 生成後に代入する任意ドラッグ通知: 0件
- 矢印集約の直接生成箇所: `矢印Factory`の2箇所だけ
- `矢印Factory.ts`: 45行
- `矢印ドラッグ履歴.ts`: 21行
- `CanvasItemFactory.ts`: 281行から236行
- BoomYack全体の100行超過コードファイル: 35個のまま

CanvasItemFactoryには付箋構築と付箋設定パネル表示が残る。次はこの二責務を別部品へ移し、Factory
本体そのものを100行以下へする。

### ユーザー: 形式的リファクタの連続実行と滑らか矢印の区間ドラッグ

> ここからはリファクタで動作が変わりそうなところはないと思うので形式的なコードの並び替え
> くらいだと思うのでいちいち止まらないで良いです。定期的に再起動してもあなたは修正
> 止まらなくていいです。あなたが作業中に私が確認するので。

`CanvasItemFactory`に残っていた付箋生成を、生成種別、集約構築、コンテキスト操作、設定パネル表示へ
分解した。コンストラクタで一度だけ各依存関係を組み立て、公開Factoryは付箋Factoryと矢印Factoryへ
委譲する。付箋の初期寸法、ドラッグ・テキスト変更履歴、矢印生成履歴、設定パネルの衝突回避、
AI・グラフ・音声操作の呼出順は変更していない。`CanvasItemFactory.ts`は236行から62行になり、
追加した4ファイルも25～60行に収まった。

> あと、滑らか矢印がハンドル間の曲線をつかんで移動みたいなことができないです。直線矢印の方は
> できるのに。掴んだ曲線は形状を変えないで移動して、両端のハンドルが追従し、その先の曲線も
> 追従するという感じにしましょう。直線矢印と同じです。

滑らか矢印の透明な操作パスへポインタードラッグを配線した。押下位置に最も近いベジェ区間を既存の
区間探索で特定し、その区間の両端点へ同じ補正済み移動量を適用する。これにより掴んだ区間の両端が
平行移動し、前後の曲線は移動した境界点へ追従する。始点または終点を含む区間では、折れ線矢印の
線分ドラッグと同様に開始時に接続を外し、終了時に接続点を再探索する。集約の必須ドラッグ通知も
開始・終了で呼ぶため、区間全体の移動が一つのUndo/Redo履歴になる。

### 検証結果（Factory分割と滑らか矢印区間ドラッグ）

- `npm run build:electron`: 成功
- `git diff --check`: 成功
- `CanvasItemFactory.ts`: 62行
- `付箋Factory.ts`: 36行
- `付箋構築.ts`: 60行
- `付箋メニュー配線.ts`: 44行
- `付箋設定パネル表示.ts`: 25行
- 滑らか矢印の変更・追加ファイル: すべて100行以下

画面確認点は、中間点0個・1個・複数個のそれぞれで各曲線区間をドラッグできること、掴んだ区間の
両端ハンドルが同量移動すること、隣接区間が追従すること、接続中の端点を含む区間が終了時に接続を
回復すること、Undo/Redoでドラッグ前後を往復できること。

### 継続リファクタ: 配置物衝突判定サービス

設定パネル配置で使う110行の`配置物衝突判定サービス`から、配置物の画面矩形変換、矩形交差、
ウィンドウ内制約を24行の`配置物画面矩形.ts`へ分離した。サービス本体は35行となった。
円周探索の初期半径150px、50px刻み、最大500px、各周12点という探索順序は維持している。
既存コードが評価していた他配置物の矩形取得も残し、ターゲット付箋だけを必須回避する判定も
変更していない。`npm run build:electron`と`git diff --check`は成功した。

### 継続リファクタ: JSON契約とパネルドラッグ

101行だった`折れ線矢印データ.ts`からJSON境界型を`折れ線矢印JSON.ts`へ分離し、既存の公開型は
再exportして利用側のimport契約を維持した。データクラス92行、JSON型12行となった。

Fudaba札検索ダイアログと付箋設定パネルに重複していたヘッダードラッグ配線を、14行の
`パネルドラッグ.ts`へ集約した。開始・終了時のカーソル、直前位置からの差分加算、表示位置更新の
順序は同じで、両UIはそれぞれ98行になった。`npm run build:electron`と`git diff --check`は成功した。

### codexあおい代表: 100行超ファイルの並列責務分割

セッション内メンバー: codexあおい + codexあかね + codexまき + codexおね

分担: codexあかね=AI・ブラウザ入出力、codexまき=セーブパネルUI、codexおね=グラフ計算、
codexあおい=キャンバスグラフ操作・コマンド群・統合検証。外部agmsg送信は行わず、共有ツリー内の
編集ディレクトリを固定した。各担当は共有スレッドと親Jimboを編集せず、統合役だけが記録と
コミットを担当した。

**判断**: 公開クラスと既存import経路を入口として残し、計算、DOM構築、ブラウザI/Oを100行以下の
部品へ移す。セーブパネル本体は575行から417行まで減ったが、保存・読込・改名・ゴミ箱表示という
状態遷移が残るため、画面確認なしで一度に分割せず次の専用単位へ残した。

**不同意**: なし。全員が機械的な移動を優先した。統合役は各報告を根拠として採用せず、並列編集が
終了した共有ツリーで`npm run build:electron`と`git diff --check`を再実行した。

**ハーネスメモ**: サブエージェントのモデルは生成API上で個別指定できない。同等モデルへ局所
コンテキストと異なる観察入口を与え、重複調査を減らす方式が有効だった。

主な分割結果は次の通り。

- AI・入出力: 対象4ファイルをすべて100行以下化
- グラフ計算: 対象ディレクトリの全TypeScriptを100行以下化
- セーブパネル: CSS、一覧、仮ゴミ箱を100行以下化。本体575行から417行
- キャンバスグラフ操作サービス: 111行から74行
- 具体的なコマンド群: 160行から81行
- BoomYack全体の100行超コードファイル: 30個から18個

### 検証結果（並列責務分割）

- `npm run build:electron`: 統合後に成功
- `git diff --check`: 統合後に成功
- 各担当の`npx tsc --noEmit`または担当時点のElectronビルド: 成功
- 実ブラウザでのAI通信、コピー、ダウンロード、保存パネル操作、乱数固定の座標比較: 未実施

### codexあおい代表: 並列責務分割の第2波

同じセッション内編成を再利用し、codexあかね=音声認識・永続化、codexまき=セーブパネル本体、
codexおね=接続点・領域分割、codexあおい=Fudaba CSS・キャンバスデータ境界を担当した。

第1波で417行まで減らした`セーブパネル.ts`は25行の公開入口となり、実装、共有状態、保存処理、
改名処理、表示制御、ゴミ箱UI制御へ分離した。セーブパネル配下の全コードファイルが100行以下に
なった。`音声認識サービス.ts`は96行、`CanvasPersistenceManager.ts`は72行となり、Web Speechの
状態遷移と、全消去・原点・配置物・接続という復元順を専用部品へ移した。

接続点、矢印接続可能なもの、領域分割管理は状態、View、方向選択、接続点群、領域セル、領域格子へ
分割した。接続・解除・座標更新順、上下左右順、方向選択の0.1慣性、4×4かつ1000pxの領域仕様を
維持した。Fudaba CSSは札参照と検索UIへ、キャンバスデータはJSON契約と配置物復元へ分離した。

### 検証結果（並列責務分割の第2波）

- `npm run build:electron`: 統合後に成功
- `git diff --check`: 統合後に成功
- BoomYack全体の100行超コードファイル: 18個から10個
- セーブパネル、接続点、領域分割の対象ディレクトリ: 全TypeScriptが100行以下
- 実マイク、永続化往復、保存パネル、接続点hover・追従、領域境界: 未実施
