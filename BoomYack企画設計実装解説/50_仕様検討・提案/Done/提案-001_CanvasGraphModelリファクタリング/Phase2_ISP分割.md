# Phase 2: `I配置物リポジトリ`のインターフェース分割（ISP）【✅ Done — commit `0a56b51`】

> **リスク**: 中（利用箇所すべてに波及するが静的型検査で安全に実施可能）

## 概要

`I配置物リポジトリ`の4責務混在をInterface Segregation Principleに基づいて分割。
`接続点`・`矢印接続可能なもの`が`I配置物リポジトリ`全体に依存していたのを`I矢印生成先`に絞り込んだ。

## Before

```typescript
// 4つの責務が1インターフェースに混在
interface I配置物リポジトリ<座標点T> {
    配置物リスト: I配置物集約[];           // コレクション管理
    add付箋(pos): 矢印接続可能付箋Old;    // ファクトリ（具象型を返す！）
    add折れ線矢印(...): 折れ線矢印集約;   // ファクトリ
    接触点を取得(pos): ...;              // 接触判定
    未接続の点ハンドルを接続点と接続をtryする(...): void;
}
```

## After

```typescript
// 矢印生成に必要な最小インターフェース
interface I矢印生成先 {
    add折れ線矢印(始点: 座標点, 終点: 座標点): void;
}
```

`接続点`・`矢印接続可能なもの`のコンストラクタ引数を`I配置物リポジトリ` → `I矢印生成先`に変更。

## 変更対象ファイル

| ファイル | 変更内容 |
|---|---|
| `配置物リポジトリ.ts` | `I矢印生成先`インターフェース追加、`I配置物リポジトリ`を廃止候補に |
| `配置物/矢印接続可能なもの/接続点.ts` | コンストラクタ引数 `I配置物リポジトリ` → `I矢印生成先` |
| `配置物/矢印接続可能なもの/矢印接続可能なもの.ts` | 依存関係インターフェース内のキー名変更 |
| `描画キャンバスView分解/CanvasItemFactory.ts` | `i配置物リポジトリ` → `i矢印生成先` |