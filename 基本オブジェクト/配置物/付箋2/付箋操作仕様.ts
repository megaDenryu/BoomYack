/** 黄色い付箋本体の外側に設ける、ドラッグ専用領域の幅。 */
export const 付箋ドラッグ操作余白Px = 30;

/** ドラッグ操作領域が利用者へ伝える状態。 */
export const 付箋ドラッグ枠線 = {
    ホバー: "2px dashed #4caf50",
    選択: "2px dashed red",
    矢印選択: "2px dashed #a5d6a7",
} as const;
